{
  "name": "carbonix-contracts",
  "version": "0.1.0",
  "description": "Smart contract implementations for Carbonix carbon credit marketplace",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Building smart contracts\" && exit 0",
    "deploy": "echo \"Deploying contracts\" && exit 0"
  },
  "dependencies": {
    "@liskhq/lisk-client": "^6.0.0",
    "@liskhq/lisk-transactions": "^6.0.0"
  },
  "devDependencies": {
    "@liskhq/lisk-framework": "^0.10.0"
  }
}